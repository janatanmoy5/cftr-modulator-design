# Generated results

Pipeline tables, docking poses, figures, logs, and the HTML report are written
here and intentionally excluded from Git. Run `./run_all.sh` to reproduce them.

The main report is generated at `results/report/index.html`.
